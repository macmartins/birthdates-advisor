import { BIRTHDAY, COUNTRY, NAME, SURNAME } from "./fields";

const IS_REQUIRED = " is required";

export const REQUIRED_NAME = NAME + IS_REQUIRED;
export const REQUIRED_SURNAME = SURNAME + IS_REQUIRED;
export const REQUIRED_COUNTRY = COUNTRY + IS_REQUIRED;
export const REQUIRED_BIRTHDAY = BIRTHDAY + IS_REQUIRED;
export const INVALID_DATE = "Invalid date";
export const MAX_DATE = "Date can't be higher than today";
